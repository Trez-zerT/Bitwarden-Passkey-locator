# Bitwarden-Passkey-locator
Python script to scan exported JSON file for passkey presence

The script expects the .json export file to be named vault.json, so rename your bitwarden_export_*.json to vault.json before running.

The output is passkeys.csv which will list the found passkeys. Needless to say, use the data and get rid of the file and vault export after verifying. 
